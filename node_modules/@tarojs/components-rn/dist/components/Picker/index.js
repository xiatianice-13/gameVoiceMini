/**
 * All:
 *   ✔ value
 *   ✔ onChange
 *   ✔ onCancel
 * Selector:
 *   ✔ range
 *   ✔ rangeKey
 *   ✔ disabled
 * MultiSelector:
 *   ✔ range
 *   ✔ rangeKey
 *   ✔ disabled
 *   ✔ onColumnChange
 * Time:
 *   ✔ start
 *   ✔ end
 *   ✔ disabled
 * Date:
 *   ✔ start
 *   ✔ end
 *   ✘ fields
 *   ✔ disabled
 * Region:
 *   ✔ customItem
 *   ✔ disabled
 *
 * @hint Picker 里面嵌套的子组件要支持绑定 onPress 事件才能弹出选择框
 */
import * as React from 'react';
import DateSelector from './date';
import MultiSelector from './multiSelector';
import RegionSelector from './region';
import Selector from './selector';
import TimeSelector from './time';
class _Picker extends React.Component {
    render() {
        const { mode, } = this.props;
        if (mode === 'selector') {
            return React.createElement(Selector, Object.assign({}, this.props));
        }
        else if (mode === 'multiSelector') {
            return React.createElement(MultiSelector, Object.assign({}, this.props));
        }
        else if (mode === 'time') {
            return React.createElement(TimeSelector, Object.assign({}, this.props));
        }
        else if (mode === 'date') {
            return React.createElement(DateSelector, Object.assign({}, this.props));
        }
        else if (mode === 'region') {
            return React.createElement(RegionSelector, Object.assign({}, this.props));
        }
        else {
            return null;
        }
    }
}
_Picker.displayName = '_Picker';
_Picker.defaultProps = {
    mode: 'selector',
};
export default _Picker;
//# sourceMappingURL=index.js.map